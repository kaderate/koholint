# Process Evidence

`PROCESS_EVIDENCE.md` defines the minimal evidence record used to study the workflow itself.

The first iteration is intentionally documentation-only: no runtime instrumentation, no new role,
and no behavior change. Sessions can begin recording entries when a process observation is worth
preserving. Once enough comparable evidence exists, MetaPlanner can use it to propose and evaluate
workflow amendments.
